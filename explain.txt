

//------------------module

1,建立一个data.js文件
 将users作为default输出。

2，index.js 文件呼叫

3,npm run webpack

4,为了不每修改一次就运行一次 npm run webpack
 可以在 "webpack": "node_modules/.bin/webpack" 后面加一个 -w
 "webpack": "node_modules/.bin/webpack -w"

 然后再运行一次npm run webpack即可
